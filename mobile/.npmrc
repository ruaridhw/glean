# expo-router@6 has optional peer deps (react-dom, react-native-web) that pull
# in versions conflicting with SDK 54's react@19.1.0. Since we only target
# native platforms, these mismatches are harmless.
legacy-peer-deps=true
