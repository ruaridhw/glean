appId: ${APP_ID}
---
- retry:
    maxRetries: 3
    commands:
      - tapOn:
          id: tabs.${tab}
          point: 50%,20%
          retryTapIfNoChange: true
      - assertVisible:
          id: ${tab}.screen
